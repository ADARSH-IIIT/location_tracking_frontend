function currentreceiver( payload ){

    // console.log(" current receiver action is running is running" , payload);
       
        return {
            type : "currentreceiver" ,
            payload : payload 
           
        }
    
    }

    export default currentreceiver